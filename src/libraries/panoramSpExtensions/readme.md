# Sample PanoramSP Extension Library

This sample is to give you an idea of how to implement the Panoram SP Extension interface so that you can expose your custom
item templates and views to PanoramSP for consumption.

This sample library is not exported in the npm package, making it safe to use as a project dependency in your own implementation.